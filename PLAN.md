# looksy — improvement backlog

> **How to run this plan (agent):** read `CLAUDE.md` first; work top-down inside a phase; each open item states *what / why / accept* — do not tick `[x]` until its accept check passes; tests never touch the network or the real cluster (fixtures); `snuff` green = done (Stop hook runs it); append friction to `FEEDBACK.md`; never `git commit`/`push` unless the user says so; ask only when two readings lead to materially different work — otherwise pick the simpler one and say so.

Actionable items distilled from `FEEDBACK.md` (field runs 2026-08-15/16). Ranked by payoff per run. Tick and move to CHANGELOG when shipped.

## P0 — reader loses time on every run
- [x] **Name the horizontal-overflow culprit.** On hscroll, list top-3 elements by right edge: `table 525px "Technique | Savings | Effort"`, `h1 "…" right=392px`. Walk elements + text ranges (`Range.getClientRects()`); dedupe parent/child (report outermost element whose own box overflows). Print under `Page:` and in the responsive block. (Asked 3×.) → files: `src/responsive-check.ts` (evaluate block ~L142: add `overflowCulprits: {tag,text,right,className}[]` to `BreakpointResult`, feed the overflow issue message + `formatResponsiveCheck` compact/verbose), `src/screenshot.ts` (~L182 `pageInfo` evaluate: collect the same top-3 when `scrollWidth > viewport`), `src/screenshot-types.ts` (`pageInfo.overflowCulprits?`), `src/cli-output.ts` (`formatPageLine`: print `  overflow: table right=525px "Technique | Savings | Effort"` lines under `Page:`), `src/analysis-summary.ts` (`responsiveCheck` line unchanged), tests `tests/unit/responsive-check.test.ts`, `tests/integration/smoke.test.ts` · accept: unit `formatResponsiveCheck` given `hasHorizontalOverflow:true, scrollWidth:525, overflowCulprits:[{tag:'table',right:525,text:'Technique | Savings | Effort'}]` → output contains `table right=525px "Technique | Savings | Effort"`; smoke `runWithStdin('<main><h1>Title</h1><table style="width:525px"><tr><td>Technique</td></tr></table></main>','--html','--width','375')` → stdout has a `Page: ` line with `⚠ +` AND a following `overflow: table right=525px` line · decide: top-3 by `getBoundingClientRect().right` over all elements + text nodes (`Range.getClientRects()`), skip descendants of an already-listed offender (outermost wins), text trimmed to 40 chars, printed in both single-run `Page:` output and the responsive block
- [x] **`fleet` Contrast Summary / "Contrast failures (…)" headers print the PNG path instead of the URL** — even without `-o`. Reuse the URL from the Batch table. → files: `src/cli.ts` (`printContrastSummary` ~L1160 — take `entries: {result,url}[]` like `printBatchSummary`, print `url` not `r.imagePath`; caller ~L980/L1105), `src/cli-output.ts` (`checkContrastExit`: use `r.url ?? r.imagePath` in the `Contrast failures (…)` header), `src/screenshot-types.ts` (add `url?: string` to `ScreenshotResult`, set in `src/screenshot.ts` from `config.url`), tests `tests/unit/formatters.test.ts` or new `tests/unit/contrast-summary.test.ts` · accept: export `formatContrastSummary(entries, label): string[]` and assert `formatContrastSummary([{url:'https://a.com/',result:{imagePath:'/tmp/x.png',contrastFailures:{aa:2,aaa:5}}}],'URLs')` returns a line starting `  https://a.com/: 2 AA, 5 AAA` and no line containing `/tmp/x.png`
- [x] **`--speed` / `--report` offenders on stdout.** `cache: 4 no-cache`, `resource-hints: 3 missing preconnect` etc. must name ≥1 offender per line (respect `--limit`); `--report` is text-only so the sidecar isn't there to fall back on. → files: `src/analysis-summary.ts` (`summarize(key, data, opts?: {limit?: number})` — for `cacheAudit`, `resourceHints`, `compression`, `imageAudit`, `bundles` append ` — <name1>, <name2>` up to `limit`, plus ` … and N more`), `src/screenshot-analysis.ts` (pass `{limit: config.listLimit}` at the `summarize(...)` calls L124–L156), tests `tests/unit/analysis-summary.test.ts` · accept: `summarize('cacheAudit', {noCacheCount:2, shortTtlCount:0, totalResources:9, entries:[{name:'a.js',issue:'hashed asset not cached',transferSize:100},{name:'b.css',issue:'static asset not cached',transferSize:50},{name:'c.png',issue:null}]}, {limit:1})` → `cache: 2 no-cache, 0 short-ttl (9 resources) — a.js … and 1 more`; `summarize('resourceHints', {existing:[],missingPreconnects:['https://fonts.gstatic.com'],unusedPreloads:[]})` → ends with ` — https://fonts.gstatic.com` · decide: offenders on the same stdout line (not extra lines), largest-first, default limit = `--limit` (10)
- [x] **`--links`: third bucket `unverifiable`** for 403/429/999 + known bot-blockers (LinkedIn 999, Cloudflare-challenged hosts); never count toward "broken". Optional `--links-allow <domains>`. → files: `src/links.ts` (`LinkResult` gets `verdict: 'ok'|'broken'|'unverifiable'`; new pure `classifyLink(status: number|null, host: string, allow: string[]): verdict` — 403/429/999 or host in `BOT_BLOCKED_HOSTS` (`linkedin.com`, `x.com`, `twitter.com`, `instagram.com`, `facebook.com`) → `unverifiable`; `formatLinks` prints a separate `Unverifiable (N)` list; `checkLinks(page, {allow})`), `src/analysis-summary.ts` (`links: 1/20 broken, 3 unverifiable`), `src/cli.ts` (+`'links-allow': {type:'string'}` → `config.linksAllow`), `src/screenshot-types.ts`, `src/cli-help.ts`, `.claude/rules/flag-behavior.md`; tests `tests/unit/links.test.ts` (new) + `tests/unit/analysis-summary.test.ts` · accept: `classifyLink(999,'www.linkedin.com',[])==='unverifiable'`, `classifyLink(403,'example.com',[])==='unverifiable'`, `classifyLink(404,'example.com',[])==='broken'`, `classifyLink(500,'x.example',['x.example'])==='unverifiable'`; `summarize('links', [{ok:false,verdict:'unverifiable',status:999},{ok:true,verdict:'ok',status:200}])` → `links: 0/2 broken, 1 unverifiable` · decide: `--links-allow` = comma-separated host suffixes always bucketed `unverifiable` (never broken); no network in tests

## P1 — false positives / inconsistent output
- [x] **Touch-target list vs. exemption rule.** Header says "+11 inline text links, exempt" but lists `a "Services" .text-sm` (nav link) and the brand link. Decide: nav `<a>` = control (label it `nav a`) or inline = exempt; be consistent. Skip visually-hidden-until-focus elements (`.skip-link`, `offsetParent === null`, clip/sr-only) — measure on focus or not at all. → files: `src/responsive-check.ts` (evaluate block: `isInlineLink` ~L210 — an `<a>` inside `nav`/`[role=navigation]`/`header` is never inline-exempt and its `tag` is reported as `nav a`; new `isFocusOnly(el)`: class `/skip[-_]?(link|to|nav)/i`, or `offsetParent === null` with `position !== 'fixed'`, or rect fully off-viewport (`right <= 0 || bottom <= 0`) → skip like `isSrOnly`; `TouchTargetDetail.tag`), `.claude/rules/flag-behavior.md` (`--responsive-check` bullets), tests `tests/integration/smoke.test.ts` (fixture pattern of the existing `--responsive-check` test) · accept: smoke `runWithStdin('<a class="skip-link" href="#m" style="position:absolute;top:-40px">Skip</a><nav><ul><li><a href="/s" style="display:inline;font-size:12px">Services</a> <span>·</span></li></ul></nav><main id=m><p>Text <a href="/x">inline</a> here.</p></main>','--html','--responsive-check')` → stdout contains `nav a` + `"Services"` under the counted controls, does NOT contain `"Skip"`, and lists `"inline"` only under the exempt heading · decide: nav `<a>` = control (labelled `nav a`); everything else keeps today's inline rule
- [x] **Button label fallback.** `button 40x40px ""` — use `aria-label` / `title` / `aria-labelledby` text when textContent is empty. → files: `src/responsive-check.ts` (evaluate block ~L256: `const text = textContent || aria-label || title || text of aria-labelledby target || ''`, apply to touch-target AND tiny-text rows), tests `tests/integration/smoke.test.ts` · accept: smoke `runWithStdin('<button aria-label="Toggle menu" style="width:40px;height:40px"></button>','--html','--responsive-check')` → stdout contains `button 40x40px "Toggle menu"` (verbose form: `<button> "Toggle menu"`), never `""`
- [x] **Contrast sampling visibility.** `contrast: 2 AA fail (150 checked, 569 unchecked)` on `--design` stdout must carry the `--contrast-limit` hint (only `--check` does today); consider auto-raising the limit for `--design-audit`. → files: `src/analysis-summary.ts` (`contrast` case: when `data.capped && data.total > checked` append ` — raise with --contrast-limit`), `src/cli.ts` (`--design-audit` block ~L278: if `values['contrast-limit']` unset → set `'400'`), `.claude/rules/flag-behavior.md`, tests `tests/unit/analysis-summary.test.ts`, `tests/unit/design-audit.test.ts` (mirror `applyDesignAudit`) · accept: `summarize('contrast', {aaFailures:2,aaaFailures:9,sampled:150,total:719,capped:true,pairs:[]})` === `contrast: 2 AA fail, 9 AAA fail (150 checked, 569 unchecked — raise with --contrast-limit)`; `applyDesignAudit({'design-audit':true})['contrast-limit']==='400'` and `applyDesignAudit({'design-audit':true,'contrast-limit':'50'})['contrast-limit']==='50'` · decide: auto-raise = 400 for `--design-audit` only, explicit flag wins
- [x] **`fleet -o <dir>/` directory mode** (trailing slash or existing dir → one slug per URL). Currently last-URL-wins single file. → files: `src/cli.ts` (fleet/`--urls` branch ~L1043–L1063: if `values.output` ends with `/` or `existsSync(values.output) && statSync().isDirectory()` → treat as `outputDir` (`mkdirSync` recursive) and clear `values.output`; same for `--pages` branch ~L943), `src/cli-utils.ts` (new pure `resolveOutputTarget(output: string|undefined): {dir?: string; file?: string}`), `src/cli-help.ts` (`-o` line: `dir/ = one file per URL`), tests `tests/unit/cli.test.ts` · accept: `resolveOutputTarget('fleet-mobile/')` → `{dir:'fleet-mobile'}`; `resolveOutputTarget('/tmp/x.png')` → `{file:'/tmp/x.png'}`; `resolveOutputTarget(<existing tmp dir>)` → `{dir}`; then batch output for `https://a.com/de/` lands at `<dir>/preview-a-com-de.png` (assert path via existing `--urls` naming code, no network) · decide: trailing slash OR existing directory both mean directory mode; batch-report.md goes into that dir too
- [x] **Hidden elements in touch-target audit** — don't measure `display:none` at the current breakpoint; rows must carry their breakpoint suffix. → files: `src/responsive-check.ts` (evaluate block ~L245: skip `isHidden(el)` (display:none / visibility:hidden / 0×0 / hidden ancestor) ALWAYS, not only with `filterVisible`; `bpNote()` ~L451: always print ` — at 375px` even for a single breakpoint), tests `tests/unit/responsive-check.test.ts` (`shows per-element touch target details` — add breakpoint suffix expectation), `tests/integration/smoke.test.ts` · accept: unit: one breakpoint result at 1440 with a 36×36 button → verbose line ends `— at 1440px`, compact line ends `— at 1440px`; smoke `runWithStdin('<button style="display:none;width:36px;height:36px">Menü</button><a href="/x" style="display:inline-block;width:20px;height:20px">go</a>','--html','--responsive-check')` → stdout contains `"go"` and NOT `"Menü"` · decide: `display:none`/`visibility:hidden` are never measured regardless of `--visible-only` (that flag keeps its meaning for opacity/off-screen only)
- [x] **`alt=""` is not "missing alt"** — never flag decorative images; print `src`/selector for real misses. → files: `src/metadata.ts` (~L90 `images[]`: add `hasAlt: img.hasAttribute('alt')`), `src/screenshot-suggest.ts` (L42: filter `!img.hasAlt` instead of `!img.alt`; `shortName` L40 — fall back to `<tag>` selector-ish `img#id`/`img.class` when src is `data:`/empty), `src/suggest.ts` (`missingAltSrcs` doc), tests `tests/unit/suggest.test.ts` / `tests/unit/feedback-round-2026-08.test.ts` · accept: unit on the exported builder in `src/screenshot-suggest.ts` (export `buildSuggestData(meta,…)`): images `[{src:'/hero.webp',alt:'',hasAlt:true,broken:false},{src:'/logo.svg',alt:'',hasAlt:false,broken:false}]` → `missingAlt===1` and `missingAltSrcs` deep-equals `['logo.svg']` (already-correct sites: `src/a11y.ts:52`, `src/report.ts:69`, `src/check.ts:546` use `img:not([alt])` — leave)
- [x] **Filter looksy's own CSP violations** (stabilisation inline style) out of the page `## Errors` block, or tag `(looksy)`. → files: `src/screenshot.ts` (`page.on('console')` L141: drop/annotate messages matching `/Refused to apply inline style|violates the following Content Security Policy directive.*style-src/` that arrive after `stabilizePage`/`addStyleTag` (`config.inject`, `--ignore`, stabilise style) — simplest: tag every CSP-inline-style error `(looksy)` when stabilise or inject is active), `src/navigate.ts` (`stabilizePage` L56 `addStyleTag` — set `nonce`? no: keep tagging), `src/metadata.ts` (`## Errors` L308 renders unchanged), new pure `isLooksyCspError(msg: string): boolean` in `src/navigate.ts`; tests `tests/unit/stabilize.test.ts` · accept: `isLooksyCspError("Refused to apply inline style because it violates the following Content Security Policy directive: \"style-src 'self' 'nonce-abc'\"…")===true`, `isLooksyCspError('Uncaught TypeError: x is not a function')===false`; and the `## Errors` line for a matched message reads `… (looksy)` · decide: annotate `(looksy)` rather than drop

## P2 — guards & ergonomics
- [x] **URL with whitespace = hard error** ("did you mean separate args?"). zsh unquoted `$URLS` produced a 120-char filename and a 404 capture. → files: `src/cli-utils.ts` (`resolveUrl` L46: `if (/\s/.test(input.trim())) throw new Error(\`URL contains whitespace: "…" — did you mean separate args? (zsh: quote or use an array)\`)`), `src/cli.ts` (fleet L232 / single L355 / `--urls` L1037 all go through `resolveUrl`, so `main().catch` prints `looksy: …` and exits 1), tests `tests/unit/cli.test.ts` (`describe('resolveUrl')`) · accept: `expect(() => resolveUrl('https://a.com/ https://a.com/de/')).toThrow(/did you mean separate args/)`; `resolveUrl('example.com')` still returns `https://example.com`
- [x] **`--dismiss-consent` always prints one status line** (`consent: clicked …` / `consent: not shown`), even with `-q`. → files: `src/cli-output.ts` (`printResult` L52: when `result.consentDismissed` is set print `consent: not shown` for `action==='none'`, `consent: clicked <target>` / `consent: hidden <target>` otherwise — outside the `quiet` guard), `src/screenshot.ts` (L163: always assign `result_consent = outcome ?? {action:'none'}` when `config.dismissConsent`), tests `tests/unit/formatters.test.ts` (spy `console.log`) · accept: `printResult({imagePath:'/x.png', consentDismissed:{action:'none'}}, {quiet:true})` logs exactly one line equal to `consent: not shown`; `{action:'clicked',target:'#accept'}` → `consent: clicked #accept`
- [x] **`-q` + `--check` → checks only** (or `--fail-only`) — today the full report body prints. → files: `src/cli-output.ts` (`printResult`: when `quiet && result.checkResults` skip `reportText`, `responsiveCheckText`, `suggestText`, `auditResults`, `analysisSummaries` — keep `Page:` line, `consent:` line and `checkResults`), `.claude/rules/flag-behavior.md` (`-q` bullet), tests `tests/unit/formatters.test.ts` · accept: `printResult({imagePath:'/x.png', pageInfo:{width:375,height:900,title:'T'}, reportText:'## Report body', checkResults:'[PASS] no-hscroll', suggestText:'1. [LOW] x'}, {quiet:true})` logs `Page: 375x900px "T"` and `[PASS] no-hscroll` and nothing containing `Report body` or `[LOW]` · decide: implement as `-q` behaviour (no new `--fail-only` flag)
- [x] **`--help` hint:** quote URLs containing `?`/`&`. → files: `src/cli-help.ts` (add under the Examples/usage block: `  Quote URLs containing ? or & (zsh globbing / word-splitting): looksy "https://a.com/?q=1"`), tests `tests/unit/cli.test.ts` (or new `tests/unit/cli-help.test.ts` importing `HELP`/the exported help string) · accept: exported help text `toContain('Quote URLs containing ? or &')`
- [x] **`image-optimizer` check** — for `?w=`/`?width=` image URLs fetch two widths, compare bytes/decoded width, report `PASS-THROUGH` (Next/Vercel/CF Images/Cloudinary/imgix). → files: new `src/image-optimizer.ts` (`extractImageOptimizer(page)`: collect `<img>` srcs matching `/[?&](w|width)=(\d+)/` or known hosts `/_next/image`, `res.cloudinary.com`, `imgix.net`, `imagedelivery.net`, `/cdn-cgi/image/`; for ≤3 distinct upstreams fetch the URL at `w=64` and `w=1080` via `page.request.get`, compare `content-length`/body bytes; `PASS-THROUGH` when sizes equal ±2%; `formatImageOptimizer`, pure `judgeOptimizer(small: number, large: number): 'OK'|'PASS-THROUGH'`, `rewriteWidth(url, w)`), `src/screenshot-analysis.ts` (register `{flag: config.imageOptimizer, key:'imageOptimizer'}`), `src/screenshot-types.ts`, `src/cli.ts` (`'image-optimizer': {type:'boolean'}`, add to the `--speed` compound ~L261), `src/analysis-summary.ts` (`image-optimizer: PASS-THROUGH (w=64 and w=1080 both 99KB)` / `OK (2 checked)`), `src/cli-help.ts`, `.claude/rules/flag-behavior.md`; tests `tests/unit/image-optimizer.test.ts` (new, pure functions only) · accept: `rewriteWidth('/_next/image?url=%2Fhero.png&w=1080&q=75', 64)` → `…&w=64&q=75`; `judgeOptimizer(101200, 101200)==='PASS-THROUGH'`, `judgeOptimizer(4200, 98800)==='OK'`; `summarize('imageOptimizer', {verdict:'PASS-THROUGH', small:{w:64,bytes:101171}, large:{w:1080,bytes:101171}})` → `image-optimizer: PASS-THROUGH (w=64 and w=1080 both 99KB)` · decide: fetch only via the Playwright request context (no Node fetch), max 3 probes per page, skipped silently when no candidate URLs
- [x] **`cache` audit false positive** — Playwright memory/disk-cache responses with empty headers must not count as `no-cache`. → files: `src/screenshot.ts` (add `page.on('response')` next to L141 collecting `{url, cacheControl: headers()['cache-control'], age, cfCacheStatus}` into a `Map` passed to `extractCacheAudit(page, headersByUrl)`), `src/cache-audit.ts` (L25–L80: classify by header when present — `immutable`/`max-age>=2592000` ⇒ cached OK; `max-age<86400` on hashed asset ⇒ `short-ttl` (fills `shortTtlCount`); only NO cache-control header AND `transferSize>0` ⇒ `no-cache`; `transferSize===0 && decodedBodySize===0` ⇒ `unknown`, never counted), tests `tests/unit/cache-audit.test.ts` (new pure `classifyCacheEntry(entry, header)`) · accept: `classifyCacheEntry({name:'chunk.a1b2c3.js',transferSize:1200,decodedBodySize:4000,isHashed:true},'public, max-age=31536000, immutable').issue===null` and `.cacheControl==='immutable (cached)'`; `classifyCacheEntry({name:'a.b1c2d3.css',transferSize:900,decodedBodySize:900,isHashed:true},'public, max-age=14400').issue==='hashed asset short-ttl (4h)'`; `classifyCacheEntry({name:'x.png',transferSize:0,decodedBodySize:0},undefined).issue===null` · decide: header-based classification replaces the transferSize heuristic wherever a header was seen
- [x] `--sweep`: one-line-per-breakpoint summary is in; keep `Page:` overflow flag consistent across sweep/fleet/single. → files: `src/cli-output.ts` (export `formatOverflowFlag(pageWidth, viewportWidth): string` → ` ⚠ hscroll +Npx wider than Wpx viewport` or `''`; use it in `formatPageLine`), `src/cli.ts` (sweep loop ~L676 and `printBatchSummary` ~L1129 call the same helper instead of inline `⚠ hscroll +…`), tests `tests/unit/feedback-round-2026-08.test.ts` (existing `formatPageLine` cases) · accept: `formatOverflowFlag(396,375)===' ⚠ hscroll +21px wider than 375px viewport'`, `formatOverflowFlag(375,375)===''`, and `formatPageLine({imagePath:'x',pageInfo:{width:396,height:800,title:'',viewportWidth:375}})` contains that exact flag · decide: single wording `⚠ hscroll +Npx wider than Wpx viewport` everywhere (sweep/batch/fleet/single); grep `hscroll +` shows one definition site
- [x] `--design-audit` sidecar should be a superset of `--design` (fonts/css-vars sections were dropped). → files: `src/cli.ts` (`--design-audit` block L277–L294 — already sets `meta`, `css-vars`, `fonts`; verify `--design` block sets nothing extra), `tests/unit/design-audit.test.ts` (its local `applyDesignAudit` mirror L8–L23 lacks `meta`/`css-vars` — either import the real expander from `src/cli.ts` (export it as `applyCompoundFlags(values)`) or update the mirror), `.claude/rules/flag-behavior.md` (`--design-audit` shorthand line already documents the superset) · accept: `applyDesignAudit({'design-audit':true})` has `meta===true`, `'css-vars'===true`, `fonts===true`, `full===true`, `compact===true`, `contrast===true`, `suggest===true` (every key `--design` sets) · decide: treat as already-implemented in `src/cli.ts`; the work is exporting the expander + updating the test mirror

## P3 — fleet plumbing (2026-08-16 hub deep-think)
- [x] Read a per-repo `fleet.yaml` (domains, pages, locales, mobile/desktop) shared with peep/texter/trusty, so `snuff --profile prod` runs the whole post-deploy set from one config instead of 4×N invocations. Keep flags as overrides. → files: new `src/fleet-config.ts` (`loadFleetConfig(path='./fleet.yaml'): {domains: string[]; pages?: string[]; locales?: string[]; viewports?: ('mobile'|'desktop')[]}` — zero-dep minimal YAML subset parser (top-level `key:` + `- item` lists / inline `[a, b]`), `expandFleetTargets(cfg): string[]` = domains × pages), `src/cli.ts` (fleet branch L222–L232: when no positionals/`--urls`/`--url-file` and `fleet.yaml` exists (or `--fleet <path>` given) → `configureFleet(expandFleetTargets(cfg), values)`; `viewports` → `--mobile`/`--multi`; flags always override), `src/cli-utils.ts` (`configureFleet`), `src/cli-help.ts`, `.claude/rules/flag-behavior.md`, README fleet section; tests `tests/unit/fleet-config.test.ts` (new; write yaml to `mkdtempSync` dir) · accept: yaml `domains: [a.com, b.com]\npages: [/, /about]\nlocales: [en, de]\nviewports: [mobile]` → `expandFleetTargets(cfg)` deep-equals `['https://a.com/','https://a.com/about','https://b.com/','https://b.com/about']`; `configureFleet([], values)` with `values.fleet='<tmp>/fleet.yaml'` sets `values.urls` to that comma-joined list and `values.mobile===true` · decide: shared schema with peep — top-level `domains`, `pages`, `locales`, `viewports`; file at repo root `./fleet.yaml`, `--fleet <path>` overrides location; looksy consumes domains+pages+viewports and ignores `locales` (pages already carry locale prefixes); `snuff --profile prod` wiring is out of scope here
- [x] `--brief` output mode (≤ 10 lines, red only) for gate/hook use. → files: `src/cli.ts` (`brief: {type:'boolean'}`; implies `quiet`; in `printBatchSummary`/single-run output route through `formatBrief`), new pure `formatBrief(entries: {url, result}[]): string[]` in `src/cli-output.ts` (one header line per URL `✗ <url> — hscroll +21px, 2 AA fail, 1 check fail` only when something is red; `✓ N URLs clean` otherwise; hard cap 10 lines, last line ` … and K more` when over), `src/cli-help.ts`, `.claude/rules/flag-behavior.md`; tests `tests/unit/formatters.test.ts` · accept: `formatBrief([{url:'https://a.com/',result:{imagePath:'x',pageInfo:{width:396,height:1,title:'',viewportWidth:375},contrastFailures:{aa:2,aaa:9}}},{url:'https://b.com/',result:{imagePath:'y',pageInfo:{width:375,height:1,title:'',viewportWidth:375},contrastFailures:{aa:0,aaa:3}}}])` deep-equals `['✗ https://a.com/ — hscroll +21px, 2 AA fail']`; 30 red URLs → `length===10` and last line matches `/… and 21 more/` · decide: red = hscroll, AA fail > 0, any `[FAIL]` check, batch failure; AAA and suggestions are not red; exit code unchanged

## P4 — fleet joins (2026-08-16 hub deep-think #2)
- [x] **JSON findings shape** — what: `--json` (single, `fleet`, `--check`) gains `findings: Finding[]` in the fleet schema (pulse README "Finding schema"): ids `visual:<url>/<check>` (`no-hscroll`, `contrast`, `heading-outline`, `og-image`, `canonical`, …), `crit` for a failed `--check` item, `warn` for audit-only red (contrast AA fail without `--check`, hscroll ⚠, touch-targets), `title` = the same one-liner `--brief` prints, `hint` = the flag to dig in (`--design`, `--speed`, `--sweep`); why: snuff `--profile prod`, pulse and brief read one shape — today each consumer would parse a different sidecar; additive to the existing JSON; → files: new `src/findings.ts` (`toFindings(entries: {url, result}[])` pure), `src/cli.ts` (attach in every `--json` branch), tests `tests/unit/findings.test.ts` · accept: a result with `checkResults` `[{name:'no-hscroll', pass:false}]` and 2 AA contrast fails → findings `[{id:'visual:<url>/no-hscroll', severity:'crit'}, {id:'visual:<url>/contrast', severity:'warn', title:/2 AA fail/}]`; all-pass → `[]`. decide: implement together with the P3 `--brief` item — same source lines.

## Keep (confirmed good in the field)
`Page: … ⚠ +Npx wider than viewport` · heading-skip names both headings · `--check` vocabulary (`no-hscroll`, `heading-outline`, `og-image`, `canonical`, …) · `fleet --mobile --design-audit -q` per-URL dashboard · `--speed` all-green in ~2s on static sites · `schema:` Merchant-shaped Product coverage · `validate-theme --text-on/--bg-tokens` · `--local-storage`/`--dismiss-consent`.

## Fleet review 2026-08-17 (hub TOOLS.md Round 4) — this section is the queue

Verdict: **USED** (180 real calls / 7 sessions / 30d, `/visual-qa` ×4; FEEDBACK 8 use-sections). Model repo for the fleet. Keep building only what FEEDBACK pulls.
- [x] **`--pages @sitemap`** — read `<origin>/sitemap.xml` (+ index sitemaps) into the page list; `--pages-limit N`; why: FEEDBACK 2026-08-16 site-fleet review — "`--pages` doesn't take a sitemap" (232-page runs listed by hand), peep asks the same (peep FEEDBACK "sitemap") → implement once as a small `sitemap.ts` that peep can copy verbatim until `clikit` exists · accept: fixture sitemap with 3 urls + 1 index → 3 routes; `--pages @sitemap --pages-limit 2` → 2. `pull/gold`
- [x] **`--fail-only`** — fleet/pages output lists only red pages; why: FEEDBACK 2026-08-16 site-fleet review — "a `--fail-only` that prints only pages with ≥1 FAIL" (232 pages, reader scrolls for the 4 that failed) · accept: 3 pages 1 red → 1 line + `2 clean`. `pull/gold`
- [x] **`[INVISIBLE]` severity** — contrast < 1.5:1 is its own line above AA fails; why: FEEDBACK 2026-08-16 site-fleet review — "ratios < 1.5 get flagged `[INVISIBLE]`" · accept: fixture with 1.2:1 → `[INVISIBLE]` line first, counted separately in `--brief`. `pull/gold`
- [x] **idle-wait cap on third-party embeds** — 30 s/page ceiling, note `(timed out waiting for network idle)`; why: FEEDBACK 2026-08-16 site-fleet review — "Contact pages took 30.2s each". `pull`
- [x] **fragment mode** — `--fragment` suppresses doc-level checks (title/canonical/hreflang) for component previews; why: FEEDBACK 2026-08-16 tally artifact demo QA — "A11y: missing lang fires on HTML fragments". `pull`
- [x] **`validate-theme` error hints at shape** — the "must have a `pairs` array" / auto-detect-failed errors should show an example config so the reader doesn't have to guess-and-retry; why: FEEDBACK 2026-08-16 site-fleet review — "the error message doesn't show the shape" · accept: `Theme config must have a "pairs" array` error text includes `e.g. {"pairs":[{"fg":"#fff","bg":"#333"}]}`. `pull`
- [x] **`--contrast-limit` auto-raise for bare `--check "contrast:aa"`** — today the 400 default only kicks in under `--design-audit` (`src/cli.ts:316`); extend it to any run whose `--check` list includes `contrast:aa`/`contrast:aaa`, since a sampled AA gate isn't a real gate; why: FEEDBACK 2026-08-16 site-fleet review — "a sampled AA gate is not a gate" · accept: `looksy --check "contrast:aa"` with no `--design-audit`/`--contrast-limit` samples 400 elements, not the 150 default; an explicit `--contrast-limit` still wins. `pull`
- [x] **`--brief` exits 1 on any ✗** — done 2026-08-17 (verify pass); consumer = snuff gate / `/ship`.
Parked (no consumer yet): fleet-level `findings[]` aggregate, `fleet.yaml` for texter/trusty. Rule: no new emitter until a consuming test exists (TOOLS.md Round 4).
